[tool.poetry]
name = "openems_benchmark"
version = "0.1.0"
description = ""
authors = ["Felix Remmel <felix.remmel@voltfang.de>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
requests = "^2.32.3"
numpy = "^2.2.4"
pandas = "^2.2.3"
openpyxl = "^3.1.2"

[tool.poetry.scripts]
benchmark = "openems_benchmark.main:main"


[tool.poetry.group.dev.dependencies]
black = "^24.10.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
