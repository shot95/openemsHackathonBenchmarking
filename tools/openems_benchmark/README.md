# Benchmarking Environment for OpenEMS

This package provides a benchmarking environment, to evaluate different metrics of a specific OpenEMS setup.

# Requirements
This project requires [poetry](https://python-poetry.org) to run.

## Usage
You have to make sure OpenEMS is up and running.

You can execute the benchmarking with `poetry run benchmark`